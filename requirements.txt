# Node.js and NPM dependencies (handled in package.json)
Node.js >= 18.0.0

# JavaScript package dependencies (install using npm or yarn)
npm install

# Required packages (add specific versions if necessary)
jest                       # Testing framework
dotenv                     # Environment variable support

# Firebase (for Firestore database)
npm install firebase

# Jest (for running tests)
npm install --save-dev jest

# Testing Library (to simulate user actions and DOM events)
npm install --save-dev @testing-library/dom @testing-library/user-event

# Additional Tools
git                        # Version control system

